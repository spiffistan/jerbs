#job-seeker-profile

  - if @job_seeker.user == current_user
    .alert.alert-info
      %p
        %strong Morn!
        Dette er din profil.
      %a.btn Endre profilen

  .page-header
    %h1= @job_seeker.name

  %h4 Mine teknologier
  - @job_seeker.technologies.each do |tech|
    %span.label= tech.shortname

  .spacer50
