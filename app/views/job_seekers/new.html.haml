%h1= t 'job_seekers.actions.add.title'

= semantic_form_for @job_seeker do |f|
  = f.inputs do
    = f.input :name
    = f.input :url

  = f.actions do
    = f.action :submit,
      :wrapper_html => { :class => 'btn-large'},
      :label => (t 'job_seekers.actions.add.submit')
