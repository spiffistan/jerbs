-# TODO split into two views?
-# TODO translate help text

- if job_seeker?
  = content_for :title, t('register.job_seeker.title')
- else
  = content_for :title, t('register.employer.title')

= devise_error_messages!

#signup
- if job_seeker?
  = render 'new_job_seeker'
- else
  = render 'new_employer'


= render '/devise/links'

- if job_seeker?
  %a{:href => '#'} Er du arbeidsgiver?
